# 🆘 おすそわけ 完全引き継ぎ書 兼 復旧手順（次のAIへ）

最終更新: 2026-07-22
オーナー: マロ（塩田さん）／ このファイルは iCloud Drive に置いてある＝Macが壊れても読める。

---

## 0. 次のAIへ：まずこれを読め

あなた（AI）は文脈ゼロでこのプロジェクトを引き継ぐ。以下を守れば一発で運用・復旧できる。

- **これは「おすそわけ」= 日本のフードロス削減マーケットプレイス（Too Good To Go 的なアプリ）。** 店が売れ残り food を割引「バッグ」で出品→客が予約購入→店頭ピックアップ。
- **本番は全部クラウドで動いている。Mac/ローカルには依存しない。** Macが壊れても客・店・売上・決済は無事。
- **あなたが最優先で守る/復元すべきは2つだけ**: ①各サービスのログイン権（＋2FA） ②Android署名鍵（このフォルダ内にある）。
- **本番への破壊的操作（prod deploy / secrets / DB直書き）はオーナー承認が要る。** 分類器がブロックする設計。コマンドは用意してオーナーに実行させる。

---

## 1. 全体像（スタックとアーキテクチャ）

- **形態**: pnpm モノレポ。フロント（Vite+React SPA）とバックエンド（Express）を **1つの Fly.io アプリで同一オリジン配信**。
- **モバイル**: Capacitor 8 で iOS/Android アプリ化。ただし**リモートロード方式**＝ネイティブは `https://osusowakejapan.org/` を WebView で開くだけ。**Web を直せばアプリにも反映されるが、必ず Fly へ deploy が要る**（ローカルビルドは使われない）。
- **DB**: Supabase Postgres（+ Drizzle ORM）。
- **決済**: Stripe Connect（手数料モデル: 店に80%入金 / プラットフォーム=店20%+ユーザー5%）。
- **言語/実行**: Node 24、TypeScript、pnpm 10.26.1。

### スタック詳細
- フロント: React 19 / Vite 7 / TailwindCSS v4 / wouter(router) / @tanstack/react-query / framer-motion / lucide-react
- バック: Express 5 / @supabase/supabase-js / drizzle-orm / firebase-admin(FCM) / @parse/node-apn(APNs)

---

## 2. 場所と識別子（最重要リファレンス）

| 項目 | 値 |
|---|---|
| ソース正本パス（Mac） | `~/Downloads/Food-Rescue-Map` （複数コピー有り。必ずcd確認） |
| GitHub（コード） | `https://github.com/mcbqdnr45h-netizen/Osusowake.git` (remote名: `github`) |
| 最新退避ブランチ | `backup/2026-07-22`（masterは古い場合あり。`git branch -a`で確認） |
| Fly アプリ名 | `osusowake-api`（region: `sin` / 常時1台起動） |
| 本番URL | `https://osusowakejapan.org/` |
| Supabase プロジェクトref | `dqybzbsdqpbfpimapnwx`（URL: `https://dqybzbsdqpbfpimapnwx.supabase.co`） |
| iOS/Android アプリID | `com.yuhi.osusowake` |
| アプリ表示名 | おすそわけ |
| Android署名鍵 | `artifacts/rescueat/android/osusowake-release.keystore`（.gitignore＝**このiCloudフォルダにバックアップ有り**） |
| 署名パスワード | `artifacts/rescueat/android/key.properties`（同上、iCloudにバックアップ有り） |
| バックエンド主要コード | `artifacts/api-server/src/`（routes/ に admin.ts, bags.ts, stores.ts, payment.ts 等） |
| フロント主要コード | `artifacts/rescueat/src/`（pages/ に AdminDashboard.tsx, StoreDashboard.tsx 等） |
| Fly設定 | `fly.toml`（ルート） |
| ビルド定義 | `Dockerfile`（ルート） |

---

## 3. インフラ＝Mac非依存（壊れても運営は止まらない）

| 何 | どこ | Mac故障時 |
|---|---|---|
| アプリ本体 | Fly.io (osusowake-api) | ✅ 稼働継続 |
| DB（店/注文/ユーザー全部） | Supabase | ✅ 無事 |
| 決済 | Stripe | ✅ 無事 |
| ドメイン/DNS | Cloudflare/レジストラ | ✅ 無事 |
| ソースコード | GitHub | ✅ pushしてあれば無事 |
| Android署名鍵 | このiCloudフォルダ | ✅ 退避済み |

→ **「Macが壊れたら運営終了」は誤解。** Macが要るのは「アプリを直す/更新する」ときだけ。

---

## 4. 🔥 Macが壊れたら：復旧手順（step by step）

### ステップ0: 慌てない
アプリは動き続けている。数日放置しても客・店・売上に影響なし。

### ステップ1: 各サービスにログインできるか確認（← 本当の生命線）
新しいMac/PCから下記に**ログインできれば全部復旧できる**。逆にログイン権を失うと詰む。
**2FA(2段階認証)アプリが壊れた端末にしか無いと入れなくなる → 各サービスの「復旧コード」を紙/別端末に控えておくこと。**

| サービス | 用途 | URL |
|---|---|---|
| GitHub (mcbqdnr45h-netizen) | ソースコード | github.com |
| Fly.io | ホスティング/デプロイ | fly.io |
| Supabase | データベース | supabase.com |
| Stripe | 決済 | dashboard.stripe.com |
| Apple Developer | iOS配信 | developer.apple.com |
| Google Play Console | Android配信 | play.google.com/console |
| Apple ID (iCloud) | このバックアップ置場 | icloud.com |
| ドメイン(Cloudflare等) | osusowakejapan.org | ― |

### ステップ2: 新Macで開発環境を復元
```bash
# ツール
xcode-select --install                 # or App StoreでXcode
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install git node pnpm flyctl
corepack enable && corepack prepare pnpm@10.26.1 --activate

# コード取得
git clone https://github.com/mcbqdnr45h-netizen/Osusowake.git
cd Osusowake
git branch -a                          # backup/... ブランチに最新がある場合あり
pnpm install --frozen-lockfile
```

### ステップ3: Android署名鍵を戻す（最重要・これが無いとAndroid更新不可）
このiCloudフォルダ内の
- `osusowake-release.keystore`
- `key.properties`
を `artifacts/rescueat/android/` にコピーで戻す。

### ステップ4: 秘密情報(.env)を復元
- テンプレは `artifacts/api-server/.env.example`（項目名が全部載っている）
- 本番の実値は **Fly にある**: `flyctl secrets list`（値は見えないが項目確認可）
- Supabaseキー→Supabaseダッシュボード、Stripeキー→Stripeダッシュボード から再取得
- ローカル開発用: `cp artifacts/api-server/.env.example artifacts/api-server/.env` して各値を埋める

### ステップ5: Flyにログイン→デプロイで通常運転復帰
```bash
flyctl auth login
cd ~/Osusowake && flyctl deploy --app osusowake-api --remote-only
```

---

## 5. 日常の開発・デプロイ手順（次のAI用オペレーションマニュアル）

### ローカル起動（2プロセス）
```bash
cd ~/Downloads/Food-Rescue-Map
# API: pnpm --filter @workspace/api-server dev   (要 .env)
# Web: pnpm --filter @workspace/rescueat dev
```

### 検証（コミット前に必ず）
```bash
pnpm run typecheck            # 型チェック（全パッケージ）
pnpm -r --if-present run build
```

### 本番デプロイ（★分類器がブロック→オーナーが実行）
```bash
cd ~/Downloads/Food-Rescue-Map && flyctl deploy --app osusowake-api --remote-only > /tmp/fly-deploy.log 2>&1; echo EXIT=$?
```
- **`| tail` を付けるな**（出力が壊れる）。必ず `> log 2>&1; echo EXIT=$?` 形式。
- **Web の修正はリモートロードなので deploy しないとアプリに反映されない。**

### 本番バンドル検証（deployが効いたか確認）
本番HTML取得→`/assets/index-*.js` を抜く→そのチャンクをcurl→`LC_ALL=C grep -aoF "探す文字列"`。
AdminDashboard/AdminStorePage は lazy chunk（別ファイル）。

### コードのバックアップ（GitHub）
```bash
git add <files> && git commit -m "..."
# ★master直pushは分類器ブロック→バックアップはブランチ名を変えて:
git push github master:refs/heads/backup/$(date +%Y-%m-%d)
```

---

## 6. 環境変数（名前だけ。実値はFly/各ダッシュボードにある）

**必須（未設定だと起動時throw）**: `PORT` `SUPABASE_URL` `SUPABASE_ANON_KEY` `SUPABASE_SERVICE_ROLE_KEY`(管理者権限・非公開) `SUPABASE_DATABASE_URL`
**任意（該当機能のみ）**: `STRIPE_SECRET_KEY` `STRIPE_WEBHOOK_SECRET` `STRIPE_WEBHOOK_SECRET_CONNECT` `OPENAI_API_KEY`(AIカテゴリ判定) `VAPID_PUBLIC_KEY` `VAPID_PRIVATE_KEY` `VAPID_SUBJECT`(Web Push) `APNS_KEY_ID` `APNS_TEAM_ID` `APNS_PRIVATE_KEY`(iOS Push) `RESEND_API_KEY` `RESEND_FROM_DOMAIN`(メール) `PARTNER_API_KEY`(公開出品API)
**ビルド時(公開値・クライアントに載る)**: `VITE_SUPABASE_URL` `VITE_SUPABASE_ANON_KEY` `VITE_MAPS_API_KEY` `VITE_GOOGLE_MAP_ID` `VITE_STRIPE_PK`（Dockerfileのbuild ARGにある）

---

## 7. ⚠️ 地雷・設計の掟（「建てるな」＝壊すと事故る）

1. **ログインメールは Supabase Auth(auth.users) にしか無い。** `users`テーブルにemail列は無い。変更は `supabaseAdmin.auth.admin.updateUserById(id,{email,email_confirm:true})`。`email_confirm:true` を外すな（確認メール送信＆未確定になる）。
2. **管理者の店舗詳細UIは2箇所ある**: ①`AdminDashboard.tsx` のインライン展開パネル（実運用のメイン導線） ②`AdminStorePage.tsx`（`/admin/store/:id`）。**店舗詳細に機能追加するときは必ず両方に入れる**（片方だけだと「機能が無い」に見える）。
3. **Capacitorの `server.url` は `https://osusowakejapan.org/` 固定。** hostnameだけにするとローカルdistが使われWeb修正が反映されない。
4. **手数料式は `payment.ts` の calcFees が真実源**（merch=COALESCE(merchandise_amount,total_price)、店payout=FLOOR(merch×0.8)）。他所で二重管理するな。
5. **定期出品**は `fly.toml` の `RECURRING_PILOT_STORE_IDS="all"` で全承認店に開放中。
6. **TailwindCSS v4 の @layer** は古いAndroid WebViewでCSS全消えする→ビルドで平坦化済み（壊すな）。
7. **Capacitor `captureInput:false` 固定**（trueにすると日本語入力不能）。
8. **AuthContext.tsx**: onAuthStateChange のキャッシュ破棄は `event==='SIGNED_OUT'` の時だけ（他のnullイベントで誤ログアウトするバグを修正済。ゲートを外すな）。
9. **requireAdmin = JWT + DB role=admin + MFA**。管理操作は `writeAuditLog` 必須。外すな。
10. **秘密は外部（第三者/公開repo）へ送るな。** バックアップ先はオーナー自身のiCloud/パスワードマネージャ/物理媒体のみ。keystoreとkey.propertiesは**セット**でないと無意味。

---

## 8. バックアップの現状（2026-07-22時点）

- **Android署名鍵**: このiCloudフォルダに退避済み（keystore + key.properties、shasum一致確認済）。
- **ソースコード**: GitHub `backup/2026-07-22` ブランチに7/1以降の3週間分をpush済み。
- **Time Machine**: 未設定（外付けドライブが無いため）。推奨: 外付けSSDを挿してON＝Mac丸ごと自動保存で最強。
- **本番DB(Supabase)**: Supabase側の自動バックアップに依存（プラン次第。復旧が心配ならSupabaseダッシュボードのBackup設定を確認）。

### 定期メンテ（月1でいい）
- コードをGitHubにpush（AIに「バックアップして」でOK）
- このフォルダのkeystoreが最新か確認
- 外付けSSDでTime Machine

---

## 9. 覚えておく合言葉
- 本番はクラウド＝**Macが壊れても運営は死なない**
- 失うと痛いのは「①各サービスのログイン権(＋2FA復旧コード)」と「②Android署名鍵」だけ
- ②はこのiCloudフォルダに退避済み
- ①だけは人力（2FA復旧コードを別保管）← これをやれば Mac故障は完全に怖くなくなる
